# Copy this file to `.env` and fill in your own values.
# NEVER commit your real .env file — add it to .gitignore.

# --- API Keys (added as each phase is built) ---
SHODAN_API_KEY= zNiDV9EeMrJLUW10MQwzYr9uErd84mpf
VIRUSTOTAL_API_KEY=your_virustotal_api_key_here
GITHUB_TOKEN=your_github_personal_access_token_here
NVD_API_KEY=your_nvd_api_key_here

# --- App settings ---
LOG_LEVEL=INFO
REPORTS_DIR=reports
DATA_DIR=data
DB_PATH=data/vulnscope.sqlite3

# --- Safety ---
# Comma-separated list of targets you are explicitly authorized to scan.
# main.py should warn (or block) if a target isn't in this list.
AUTHORIZED_TARGETS=192.168.1.10,lab.local
